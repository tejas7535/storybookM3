{
  "name": "shared-ui-feedback-banner",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/shared/ui/feedback-banner/src",
  "prefix": "schaeffler",
  "projectType": "library",
  "tags": ["scope:shared", "type:ui"],
  "targets": {
    "build": {
      "executor": "@nx/angular:package",
      "options": {
        "tsConfig": "libs/shared/ui/feedback-banner/tsconfig.lib.json",
        "project": "libs/shared/ui/feedback-banner/ng-package.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "libs/shared/ui/feedback-banner/tsconfig.lib.prod.json"
        }
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/shared/ui/feedback-banner/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "configurations": {
        "ci": {
          "format": "json",
          "outputFile": "eslint/libs/shared/ui/feedback-banner/eslint.json",
          "force": true
        }
      }
    },
    "sonar": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "sonar-scanner -Dsonar.token=${SONAR_TOKEN} -Dproject.settings=libs/shared/ui/feedback-banner/sonar-project.properties"
        ],
        "parallel": false
      },
      "outputs": []
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "pnpm publish dist/libs/shared/ui/feedback-banner --tag latest"
          }
        ]
      }
    },
    "version": {
      "executor": "@jscutlery/semver:version",
      "options": {
        "preset": "conventional",
        "baseBranch": "master",
        "tagPrefix": "feedback-banner-v",
        "changelogHeader": "# Release Notes of Library Shared UI Feedback banner",
        "commitMessageFormat": "chore({projectName}): 🚀 release version ${version} [CI SKIP]",
        "noVerify": true,
        "postTargets": []
      }
    }
  }
}
