{
  "name": "shared-ui-file-upload",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/shared/ui/file-upload/src",
  "prefix": "schaeffler",
  "tags": ["scope:shared", "type:ui"],
  "implicitDependencies": [],
  "targets": {
    "build": {
      "executor": "@nx/angular:ng-packagr-lite",
      "options": {
        "tsConfig": "libs/shared/ui/file-upload/tsconfig.lib.json",
        "project": "libs/shared/ui/file-upload/ng-package.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "libs/shared/ui/file-upload/tsconfig.lib.prod.json"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "configurations": {
        "ci": {
          "format": "json",
          "outputFile": "eslint/libs/shared/ui/file-upload/eslint.json",
          "force": true
        }
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/libs/shared/ui/file-upload"],
      "options": {
        "jestConfig": "libs/shared/ui/file-upload/jest.config.ts"
      }
    },
    "sonar": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "sonar-scanner -Dsonar.token=${SONAR_TOKEN} -Dproject.settings=libs/shared/ui/file-upload/sonar-project.properties"
        ],
        "parallel": false
      },
      "outputs": []
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "pnpm publish dist/libs/shared/ui/file-upload --tag latest"
          }
        ]
      }
    },
    "version": {
      "executor": "@jscutlery/semver:version",
      "options": {
        "preset": "conventional",
        "baseBranch": "master",
        "tagPrefix": "file-upload-v",
        "changelogHeader": "# Release Notes of Library Shared UI File Upload",
        "commitMessageFormat": "chore({projectName}): 🚀 release version ${version} [CI SKIP]",
        "noVerify": true,
        "postTargets": []
      }
    }
  }
}
