export class StatusBar {
  rows: {
    total: number;
    selected: number;
  };
  netValue: {
    selected: number;
    total: number;
  };
  gpi: {
    total: number;
    selected: number;
  };
  gpm: {
    total: number;
    selected: number;
  };
}
