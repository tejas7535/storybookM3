import { ColDef } from '@ag-grid-community/all-modules';

export const DEFAULT_COLUMN_DEFS: ColDef = {
  editable: false,
  flex: 1,
  initialWidth: 150,
  resizable: true,
  sortable: true,
  filter: true,
  floatingFilter: true,
};
