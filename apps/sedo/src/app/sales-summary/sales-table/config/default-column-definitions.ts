import { ColDef } from '@ag-grid-community/all-modules';

export const DEFAULT_COLUMN_DEFINITION: ColDef = {
  sortable: true,
  filter: true,
  floatingFilter: true,
  resizable: true,
  enablePivot: false,
};
