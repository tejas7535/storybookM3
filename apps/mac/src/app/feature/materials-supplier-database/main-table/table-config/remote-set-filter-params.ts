import { DISTINCT_VALUE_GETTER } from './helpers';

export const REMOTE_SET_FILTER_PARAMS = {
  values: DISTINCT_VALUE_GETTER,
  buttons: ['apply', 'reset'],
  defaultToNothingSelected: true,
  closeOnApply: true,
};
